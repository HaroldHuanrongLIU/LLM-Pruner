[project]
name = "llm-pruner"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "torch",
    "transformers>=4.28.1,<4.40.0",
    "datasets",
    "sentencepiece",
    "accelerate",
    "protobuf",
]
